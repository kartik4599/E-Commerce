import classes from "./Cart.module.css";

const Cart =()=>{
    return(
        <button className={classes.button}>
            Cart
            <span>1</span>
        </button>
    );
}

export default Cart;